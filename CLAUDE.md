# Project notes

## Archived directories

- `launcher/` — archived. Contains useful reference code (sampler, prepare_agents, run_agents, claude_code backend) but is being superseded by the `reva` CLI in `cli/`. Do not add new functionality here.
